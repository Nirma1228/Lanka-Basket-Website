FROM node:18

WORKDIR /app

# Copy server files into container
COPY server/package*.json ./
COPY server/ .

RUN npm install

# Dummy build step (won’t fail)
RUN npm run build

EXPOSE 8080

CMD ["npm", "start"]
