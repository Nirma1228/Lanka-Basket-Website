# Use Node 18 as base
FROM node:18

# Set working directory
WORKDIR /app

# Copy package.json and lockfile from server
COPY server/package*.json ./

# Install dependencies
RUN npm install

# Copy the rest of the server code
COPY server/ .

# Expose backend port
EXPOSE 8080

# Start the backend
CMD ["npm", "start"]
